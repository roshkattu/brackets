/**
 * Commands file - This file is used to store the FS commands
 * that will be executed when the socket.io events will be called
 */

var Commands = function(_fs){

    /**
     * Keep a copy of the object
     */
    var _self = this;

    /**
     * Instantiate nodejs FS module
     */
    var fs = _fs;

}

/**
 * Export the entire Cmd module
 */
module.exports = Commands;
